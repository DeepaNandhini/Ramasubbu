#include<stdio.h>
#include<string.h>
#include<malloc.h>
main()
{
int len;
char *str1,*str2;
str1=(char*)malloc(sizeof(char));
str2=(char*)malloc(sizeof(char));
scanf("%[^\n]s",str1);
len=str1.length();
for(i=len;i>0;i--)
{
  str2[k]=str[i];
  k++;
}
printf("%s",str2);
}
